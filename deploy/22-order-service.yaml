apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: mining-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: order-service
  template:
    metadata:
      labels:
        app: order-service
    spec:
      containers:
        - name: order-service
          image: order-service:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: SERVICE_NAME
              value: "order"
            - name: SERVICE_VERSION
              value: "v1.0.0"
            - name: CONF
              value: "/app/configs/config.yaml"
          ports:
            - containerPort: 8103
            - containerPort: 9103
          volumeMounts:
            - name: config
              mountPath: /app/configs
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: config
          configMap:
            name: service-configs
            items:
              - key: order-config.yaml
                path: config.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: order-service
  namespace: mining-platform
spec:
  selector:
    app: order-service
  ports:
    - port: 8103
      targetPort: 8103
      name: http
    - port: 9103
      targetPort: 9103
      name: grpc
