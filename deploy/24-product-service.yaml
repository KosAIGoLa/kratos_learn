apiVersion: apps/v1
kind: Deployment
metadata:
  name: product-service
  namespace: mining-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: product-service
  template:
    metadata:
      labels:
        app: product-service
    spec:
      containers:
        - name: product-service
          image: product-service:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: SERVICE_NAME
              value: "product"
            - name: SERVICE_VERSION
              value: "v1.0.0"
            - name: CONF
              value: "/app/configs/config.yaml"
          ports:
            - containerPort: 8105
            - containerPort: 9105
          volumeMounts:
            - name: config
              mountPath: /app/configs
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: config
          configMap:
            name: service-configs
            items:
              - key: product-config.yaml
                path: config.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: product-service
  namespace: mining-platform
spec:
  selector:
    app: product-service
  ports:
    - port: 8105
      targetPort: 8105
      name: http
    - port: 9105
      targetPort: 9105
      name: grpc
