apiVersion: apps/v1
kind: Deployment
metadata:
  name: system-service
  namespace: mining-platform
spec:
  replicas: 1
  selector:
    matchLabels:
      app: system-service
  template:
    metadata:
      labels:
        app: system-service
    spec:
      containers:
        - name: system-service
          image: system-service:latest
          imagePullPolicy: IfNotPresent
          env:
            - name: SERVICE_NAME
              value: "system"
            - name: SERVICE_VERSION
              value: "v1.0.0"
            - name: CONF
              value: "/app/configs/config.yaml"
          ports:
            - containerPort: 8106
            - containerPort: 9106
          volumeMounts:
            - name: config
              mountPath: /app/configs
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: config
          configMap:
            name: service-configs
            items:
              - key: system-config.yaml
                path: config.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: system-service
  namespace: mining-platform
spec:
  selector:
    app: system-service
  ports:
    - port: 8106
      targetPort: 8106
      name: http
    - port: 9106
      targetPort: 9106
      name: grpc
