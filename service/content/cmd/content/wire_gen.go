// Code generated by Wire. DO NOT EDIT.

//go:generate go run -mod=mod github.com/google/wire/cmd/wire
//go:build !wireinject
// +build !wireinject

package main

import (
	"content/internal/biz"
	"content/internal/conf"
	"content/internal/data"
	"content/internal/server"
	"content/internal/service"
	"github.com/go-kratos/kratos/v2"
	"github.com/go-kratos/kratos/v2/log"
)

import (
	_ "go.uber.org/automaxprocs"
)

// Injectors from wire.go:

// wireApp init kratos application.
func wireApp(confServer *conf.Server, confData *conf.Data, logger log.Logger) (*kratos.App, func(), error) {
	db := data.NewDB(confData)
	client := data.NewRedis(confData)
	dataData, cleanup, err := data.NewData(confData, logger, db, client)
	if err != nil {
		return nil, nil, err
	}
	newsRepo := data.NewNewsRepo(dataData, logger)
	bannerRepo := data.NewBannerRepo(dataData, logger)
	contentUsecase := biz.NewContentUsecase(newsRepo, bannerRepo, logger)
	contentService := service.NewContentService(contentUsecase, logger)
	grpcServer := server.NewGRPCServer(confServer, contentService, logger)
	httpServer := server.NewHTTPServer(confServer, contentService, logger)
	app := newApp(logger, grpcServer, httpServer)
	return app, func() {
		cleanup()
	}, nil
}
